# 微信公众号配置
# 在微信公众号平台获取：https://mp.weixin.qq.com/
WECHAT_APP_ID=your_app_id_here
WECHAT_APP_SECRET=your_app_secret_here

# 服务器配置
# 服务端口，默认 3000，如果修改需要同时更新 docker-compose.yml 中的端口映射
PORT=3000

# 运行环境：production 或 development
NODE_ENV=production

# 图片存储配置
# 本地图片目录的路径，默认为 /app/images
# 注意：此路径是容器内的路径，不要修改
HOST_IMAGE_PATH=/app/images

# 日志配置
# 日志级别：debug, info, warn, error
LOG_LEVEL=info

# 安全配置
# 是否启用 CORS，默认 true
ENABLE_CORS=true

# 超时配置（毫秒）
# API 请求超时时间
API_TIMEOUT=30000

# 缓存配置
# 是否启用缓存，默认 true
ENABLE_CACHE=true
# 缓存过期时间（秒）
CACHE_TTL=3600 